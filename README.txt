README:

El archivo con el que se tiene que empezar es el que se llama index.html.

He vinculado variables entre distintos archivos con "sessionStorage".
Si ponia el "final.html" dentro las carpetas como los otros, no se porque pero, me daba error, no leia el objeto JSON.